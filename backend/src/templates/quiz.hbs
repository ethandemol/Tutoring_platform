<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{title}}</title>

    <style>
        @page {
            size: A4;
            margin: 1cm;
        }
        
        body {
            font-family: 'Arial', sans-serif;
            font-size: 11pt;
            line-height: 1.3;
            margin: 0;
            padding: 0;
            color: #333;
        }
        
        .header {
            text-align: center;
            border-bottom: 2px solid #333;
            padding-bottom: 10px;
            margin-bottom: 20px;
        }
        
        .header h1 {
            font-size: 16pt;
            font-weight: bold;
            margin: 0 0 5px 0;
        }
        
        .header .course-info {
            font-size: 9pt;
            color: #666;
        }
        
        .header .date {
            font-size: 9pt;
            color: #666;
            margin-top: 5px;
        }
        
        .instructions {
            background-color: #f8f9fa;
            border: 1px solid #dee2e6;
            padding: 12px;
            margin-bottom: 20px;
            border-radius: 5px;
            font-size: 10pt;
        }
        
        .instructions h3 {
            margin: 0 0 8px 0;
            font-size: 11pt;
            color: #495057;
        }
        
        .instructions ul {
            margin: 0;
            padding-left: 18px;
        }
        
        .instructions li {
            margin-bottom: 3px;
        }
        
        .quiz-grid {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 20px;
            margin-bottom: 20px;
        }
        
        .question {
            padding: 15px;
            page-break-inside: avoid;
            background-color: #fff;
        }
        
        .question-header {
            font-weight: bold;
            margin-bottom: 8px;
            color: #495057;
            font-size: 10pt;
        }
        
        .question-text {
            margin-bottom: 12px;
            font-size: 10pt;
        }
        
        .options {
            margin-left: 15px;
        }
        
        .option {
            margin-bottom: 8px;
            display: block;
            clear: both;
        }
        
        .option-label {
            font-weight: bold;
            margin-right: 8px;
            display: inline-block;
            min-width: 20px;
            font-size: 10pt;
        }
        
        .option-text {
            font-size: 10pt;
            display: inline;
        }
        

        
        .footer {
            position: fixed;
            bottom: 0;
            left: 0;
            right: 0;
            text-align: center;
            font-size: 9pt;
            color: #666;
            padding: 8px 0;
            border-top: 1px solid #ccc;
            background-color: #fff;
        }
        
        .page-number {
            position: fixed;
            bottom: 8px;
            right: 10px;
            font-size: 9pt;
            color: #666;
        }
        
        .answer-key-section {
            margin-top: 40px;
            page-break-before: always;
            border-top: 3px solid #333;
            padding-top: 20px;
        }
        
        .answer-key-header {
            text-align: center;
            margin-bottom: 30px;
        }
        
        .answer-key-header h2 {
            font-size: 24pt;
            font-weight: bold;
            color: #2c3e50;
            margin: 0;
            text-transform: uppercase;
            letter-spacing: 2px;
        }
        
        .answer-key-content {
            line-height: 1.6;
            font-size: 12pt;
        }
        
        /* Enhanced answer key styling */
        .answer-key-section {
            margin-top: 40px;
            page-break-before: always;
            border-top: 3px solid #333;
            padding-top: 20px;
        }
        
        .answer-key-header {
            text-align: center;
            margin-bottom: 30px;
        }
        
        .answer-key-header h2 {
            font-size: 24pt;
            font-weight: bold;
            color: #2c3e50;
            margin: 0;
            text-transform: uppercase;
            letter-spacing: 2px;
        }
        
        .answer-key-content {
            line-height: 1.6;
            font-size: 12pt;
        }
        
        /* Answer key section styling */
        .answer-section {
            margin-bottom: 30px;
        }
        
        .answer-section-header {
            font-size: 16pt;
            font-weight: bold;
            color: #2c3e50;
            margin-bottom: 20px;
            padding-bottom: 8px;
            border-bottom: 2px solid #3498db;
            text-transform: uppercase;
            letter-spacing: 1px;
        }
        
        .answer-item {
            margin-bottom: 25px;
            padding: 15px;
            background-color: #f8f9fa;
            border-left: 4px solid #3498db;
            border-radius: 5px;
            page-break-inside: avoid;
        }
        
        .answer-question-number {
            font-size: 14pt;
            font-weight: bold;
            color: #2c3e50;
            margin-bottom: 10px;
            display: flex;
            align-items: center;
        }
        
        .answer-question-number::before {
            content: "Question ";
            color: #7f8c8d;
        }
        
        .answer-content {
            margin-left: 20px;
        }
        
        .answer-field {
            margin-bottom: 12px;
        }
        
        .answer-field-label {
            font-weight: bold;
            color: #27ae60;
            margin-right: 8px;
            text-transform: uppercase;
            font-size: 10pt;
            letter-spacing: 0.5px;
        }
        
        .answer-field-value {
            font-weight: normal;
            color: #2c3e50;
        }
        
        .answer-explanation {
            margin-top: 8px;
            padding: 10px;
            background-color: #ecf0f1;
            border-radius: 4px;
            border-left: 3px solid #3498db;
        }
        
        .answer-key-points {
            margin-top: 10px;
        }
        
        .answer-key-points-label {
            font-weight: bold;
            color: #e67e22;
            margin-bottom: 5px;
            text-transform: uppercase;
            font-size: 10pt;
            letter-spacing: 0.5px;
        }
        
        .answer-key-points-list {
            margin-left: 20px;
        }
        
        .answer-key-points-list li {
            margin-bottom: 5px;
            color: #34495e;
        }
        
        .answer-grading-criteria {
            margin-top: 10px;
        }
        
        .answer-grading-criteria-label {
            font-weight: bold;
            color: #e74c3c;
            margin-bottom: 5px;
            text-transform: uppercase;
            font-size: 10pt;
            letter-spacing: 0.5px;
        }
        
        .answer-grading-criteria-list {
            margin-left: 20px;
        }
        
        .answer-grading-criteria-list li {
            margin-bottom: 5px;
            color: #34495e;
        }
        
        /* Multiple choice answer styling */
        .answer-multiple-choice .answer-field-value {
            font-weight: bold;
            font-size: 14pt;
            color: #27ae60;
        }
        
        /* Short answer and essay styling */
        .answer-sample-answer {
            font-style: italic;
            color: #34495e;
            background-color: #f1f2f6;
            padding: 8px;
            border-radius: 4px;
            margin-bottom: 8px;
        }
        
        /* Responsive grid for smaller screens */
        @media (max-width: 800px) {
            .quiz-grid {
                grid-template-columns: 1fr;
            }
        }
        
        /* Ensure proper page breaks */
        .question {
            page-break-inside: avoid;
        }
        
        .instructions {
            page-break-inside: avoid;
        }
        
        /* Print styles */
        @media print {
            .question {
                break-inside: avoid;
            }
        }
    </style>
</head>
<body>
    <div class="header">
        <h1>{{title}}</h1>
        <div class="course-info">{{courseName}}</div>
        <div class="date">{{generatedAt}}</div>
    </div>
    
    {{#if instructions}}
    <div class="instructions">
        <h3>Instructions:</h3>
        <ul>
            {{#each instructions}}
            <li>{{this}}</li>
            {{/each}}
        </ul>
    </div>
    {{/if}}
    
    <div class="quiz-grid">
        {{#each questions}}
        <div class="question">
            <div class="question-header">
                Question {{inc @index}}
            </div>
            
            <div class="question-text">
                {{{q}}}
            </div>
            
            {{#if options}}
                <div class="options">
                    {{#each options}}
                    <div class="option">
                        <span class="option-label">{{@key}})</span>
                        <span class="option-text">{{{this}}}</span>
                    </div>
                    {{/each}}
                </div>
            {{/if}}
        </div>
        {{/each}}
    </div>
    
    {{#if answerKeyContent}}
    <div class="answer-key-section">
        <div class="answer-key-header">
            <h2>ANSWER KEY</h2>
        </div>
        
        {{#if parsedAnswerKey}}
            {{#each parsedAnswerKey}}
            <div class="answer-section">
                <div class="answer-section-header">
                    {{#ifEquals type "multiple_choice"}}
                        MULTIPLE CHOICE SOLUTIONS
                    {{else ifEquals type "short_answer"}}
                        SHORT ANSWER SOLUTIONS
                    {{else ifEquals type "essay"}}
                        ESSAY SOLUTIONS
                    {{else ifEquals type "free_response"}}
                        FREE RESPONSE SOLUTIONS
                    {{else}}
                        {{type}} SOLUTIONS
                    {{/ifEquals}}
                </div>
                
                {{#each answers}}
                <div class="answer-item {{#ifEquals ../type "multiple_choice"}}answer-multiple-choice{{/ifEquals}}">
                    <div class="answer-question-number">{{questionNumber}}</div>
                    <div class="answer-content">
                        {{#if answer}}
                        <div class="answer-field">
                            <span class="answer-field-label">
                                {{#ifEquals ../type "multiple_choice"}}ANSWER{{else}}SAMPLE ANSWER{{/ifEquals}}:
                            </span>
                            <span class="answer-field-value">{{answer}}</span>
                        </div>
                        {{/if}}
                        
                        {{#if explanation}}
                        <div class="answer-explanation">
                            <span class="answer-field-label">EXPLANATION:</span>
                            <span class="answer-field-value">{{explanation}}</span>
                        </div>
                        {{/if}}
                        
                        {{#if keyPoints.length}}
                        <div class="answer-key-points">
                            <div class="answer-key-points-label">KEY POINTS:</div>
                            <ul class="answer-key-points-list">
                                {{#each keyPoints}}
                                <li>{{this}}</li>
                                {{/each}}
                            </ul>
                        </div>
                        {{/if}}
                        
                        {{#if gradingCriteria.length}}
                        <div class="answer-grading-criteria">
                            <div class="answer-grading-criteria-label">GRADING CRITERIA:</div>
                            <ul class="answer-grading-criteria-list">
                                {{#each gradingCriteria}}
                                <li>{{this}}</li>
                                {{/each}}
                            </ul>
                        </div>
                        {{/if}}
                    </div>
                </div>
                {{/each}}
            </div>
            {{/each}}
        {{else}}
            <div class="answer-key-content">
                {{{answerKeyContent}}}
            </div>
        {{/if}}
    </div>
    {{/if}}
    
    <div class="footer">
        <div class="page-number">Page <span class="page"></span></div>
    </div>
    
    <script>
        // Add page numbers
        document.addEventListener('DOMContentLoaded', function() {
            const pages = document.querySelectorAll('.page');
            pages.forEach((page, index) => {
                page.textContent = index + 1;
            });
        });
    </script>
</body>
</html> 