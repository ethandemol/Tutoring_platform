<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{title}}</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/katex@0.16.9/dist/katex.min.css">
    <script src="https://cdn.jsdelivr.net/npm/katex@0.16.9/dist/katex.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/katex@0.16.9/dist/contrib/auto-render.min.js"></script>
    <style>
        @page {
            size: A4;
            margin: 0.5cm;
        }
        
        body {
            font-family: 'Arial', sans-serif;
            font-size: 10pt;
            line-height: 1.2;
            margin: 0;
            padding: 0;
            color: #333;
        }
        
        .header {
            text-align: center;
            padding: 10px 0;
            margin-bottom: 15px;
            border-bottom: 1px solid #ccc;
        }
        
        .header h1 {
            font-size: 14pt;
            font-weight: bold;
            margin: 0 0 3px 0;
        }
        
        .header .course-info {
            font-size: 8pt;
            color: #666;
        }
        
        .header .date {
            font-size: 8pt;
            color: #666;
            margin-top: 3px;
        }
        
        .flashcard-page {
            display: grid;
            grid-template-columns: 1fr 1fr;
            grid-template-rows: 1fr 1fr;
            gap: 15px;
            height: calc(100vh - 80px);
            page-break-after: always;
        }
        
        .flashcard {
            border: 2px dashed #999;
            border-radius: 8px;
            padding: 15px;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            text-align: center;
            background-color: #fff;
            position: relative;
            min-height: 120px;
        }
        
        .flashcard-front {
            border-color: #4CAF50;
        }
        
        .flashcard-back {
            border-color: #2196F3;
        }
        
        .flashcard-number {
            position: absolute;
            top: 5px;
            left: 5px;
            font-size: 8pt;
            color: #999;
            font-weight: bold;
        }
        
        .flashcard-content {
            flex: 1;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            width: 100%;
        }
        
        .flashcard-question {
            font-weight: bold;
            margin-bottom: 10px;
            font-size: 11pt;
        }
        
        .flashcard-answer {
            font-size: 10pt;
            color: #666;
        }
        
        .flashcard-label {
            position: absolute;
            bottom: 5px;
            right: 5px;
            font-size: 7pt;
            color: #999;
            font-weight: bold;
        }
        
        .katex {
            font-size: 0.9em;
        }
        
        .katex-display {
            margin: 0.3em 0;
            text-align: center;
        }
        
        /* Cut lines for scissors */
        .cut-line {
            position: absolute;
            background-color: #ccc;
        }
        
        .cut-line-horizontal {
            height: 1px;
            width: 100%;
            top: 50%;
            left: 0;
        }
        
        .cut-line-vertical {
            width: 1px;
            height: 100%;
            left: 50%;
            top: 0;
        }
        
        /* Instructions page */
        .instructions-page {
            page-break-after: always;
            padding: 20px;
            background-color: #f8f9fa;
            border: 1px solid #dee2e6;
            border-radius: 5px;
            margin-bottom: 20px;
        }
        
        .instructions-page h2 {
            font-size: 12pt;
            margin: 0 0 10px 0;
            color: #495057;
        }
        
        .instructions-page ul {
            margin: 0;
            padding-left: 20px;
        }
        
        .instructions-page li {
            margin-bottom: 5px;
            font-size: 9pt;
        }
        
        /* Print styles */
        @media print {
            .flashcard {
                break-inside: avoid;
            }
            
            .flashcard-page {
                break-inside: avoid;
            }
        }
        
        /* Ensure proper page breaks */
        .flashcard-page {
            page-break-inside: avoid;
        }
    </style>
</head>
<body>
    <div class="header">
        <h1>{{title}}</h1>
        <div class="course-info">{{courseName}}</div>
        <div class="date">{{generatedAt}}</div>
    </div>
    
    <!-- Instructions page -->
    <div class="instructions-page">
        <h2>How to use these flashcards:</h2>
        <ul>
            <li>Cut along the dashed lines to separate each flashcard</li>
            <li>Green borders = Question side (front)</li>
            <li>Blue borders = Answer side (back)</li>
            <li>Study by reading the question, then flipping to see the answer</li>
            <li>Shuffle the cards for better retention</li>
        </ul>
    </div>
    
    {{#each flashcards}}
    <div class="flashcard-page">
        <!-- Front of flashcard -->
        <div class="flashcard flashcard-front">
            <div class="flashcard-number">{{inc @index}}</div>
            <div class="flashcard-content">
                <div class="flashcard-question">
                    {{{katex q}}}
                </div>
            </div>
            <div class="flashcard-label">FRONT</div>
        </div>
        
        <!-- Back of flashcard -->
        <div class="flashcard flashcard-back">
            <div class="flashcard-number">{{inc @index}}</div>
            <div class="flashcard-content">
                <div class="flashcard-answer">
                    {{{katex a}}}
                </div>
            </div>
            <div class="flashcard-label">BACK</div>
        </div>
        
        {{#if @even}}
        <!-- Next flashcard front -->
        {{#if (lookup ../flashcards (add @index 1))}}
        <div class="flashcard flashcard-front">
            <div class="flashcard-number">{{add @index 2}}</div>
            <div class="flashcard-content">
                <div class="flashcard-question">
                    {{{katex (lookup (lookup ../flashcards (add @index 1)) 'q')}}}
                </div>
            </div>
            <div class="flashcard-label">FRONT</div>
        </div>
        
        <!-- Next flashcard back -->
        <div class="flashcard flashcard-back">
            <div class="flashcard-number">{{add @index 2}}</div>
            <div class="flashcard-content">
                <div class="flashcard-answer">
                    {{{katex (lookup (lookup ../flashcards (add @index 1)) 'a')}}}
                </div>
            </div>
            <div class="flashcard-label">BACK</div>
        </div>
        {{else}}
        <!-- Empty spaces for odd number of flashcards -->
        <div class="flashcard" style="border: 1px solid #eee; background-color: #f9f9f9;"></div>
        <div class="flashcard" style="border: 1px solid #eee; background-color: #f9f9f9;"></div>
        {{/if}}
        {{/if}}
    </div>
    {{/each}}
    
    <script>
        // Render KaTeX math expressions
        renderMathInElement(document.body, {
            delimiters: [
                {left: '$$', right: '$$', display: true},
                {left: '\\(', right: '\\)', display: false}
            ],
            throwOnError: false
        });
    </script>
</body>
</html> 